name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Stage existing static files
        run: |
          mkdir _site
          cp index.html 404.html favicon.svg _site/
          cp -a assets __grok _site/
          touch _site/.nojekyll
          python3 -c "from pathlib import Path; d=Path('_site/index.html').read_bytes(); assert b'\x00' not in d, 'NUL in index.html'; print('index.html', len(d), 'bytes, no NUL')"
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
